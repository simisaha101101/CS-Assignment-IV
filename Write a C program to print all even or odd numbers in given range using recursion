#include<stdio.h>
void printEvenOdd(int cur,int limit)
int main()
{
int lowerlimit,upperlimit;
printf("Enter lower limit:");
scanf("%d",&lowerlimit);
printf("Enter upper limit:");
scanf("%d",&upperlimit);
printf("Even/odd numbers from %d to %d are:",lowerlimit,upperlimit);
print EvenOdd(lowerlimit,upperprint);
return0;
}
void print EvenOdd(int cur,int lim)
{
if(cur>limit)
return;
printf("%d",cur);
print EvenOdd(cur+2,limit);
}
